services:
  - type: web
    name: life-changer-ai-tracker
    env: docker
    region: frankfurt  # You can change this to your preferred region
    plan: free
    branch: main
    envVars:
      - key: PORT
        value: 3000